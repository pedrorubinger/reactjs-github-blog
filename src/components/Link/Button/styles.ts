import styled from "styled-components"
import { LinkBaseStyles } from "../styles"

export const StyledLinkButton = styled.button`
	${LinkBaseStyles};

	background: none;

	border: none;
	border-bottom: 1px solid transparent;
`
